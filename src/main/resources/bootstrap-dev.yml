spring:
  cloud:
    config:
      label: master
      profile: dev
      name: cloud-gateway
      discovery:
        service-id: config-server
        enabled: true
      username: admin
      password: admin